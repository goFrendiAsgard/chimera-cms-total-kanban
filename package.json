{
  "name": "total-kanban",
  "version": "0.0.0",
  "description": "Trello + Wordpress + Everything else",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node setup.js install",
    "uninstall": "node setup.js uninstall"
  },
  "author": "goFrendi",
  "license": "MIT",
  "dependencies": {
    "markdown": "^0.5.0"
  }
}
